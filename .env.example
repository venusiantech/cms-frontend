# Backend API URL (include /api at the end)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Platform domains (main dashboard URLs)
# Supports comma-separated list for multiple domains
# Local: cms.local
# Production: jaal.com,cms-frontend-production-750d.up.railway.app
NEXT_PUBLIC_PLATFORM_DOMAIN=cms.local
